# Scripts

| Script | Command | Purpose |
|--------|---------|---------|
| `build-spatial-tiles.js` | `just build-tiles` | Partitions MotherDuck data into geographic tiles |
| `deploy.js` | `just deploy` | Uploads build to Digital Ocean Spaces |
| `generate-geojson.js` | (build step) | Generates points.geojson for map |
| `release.js` | `just release` | Version bumping |
| `count-assets.js` | `just count-assets` | Counts assets in MotherDuck |
