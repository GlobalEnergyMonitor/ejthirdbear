# No API keys required! Embeddings run locally via @huggingface/transformers.
# The model (~23MB) downloads automatically on first use and caches to disk.
#
# Optional: set ORIGIN for production CORS
# ORIGIN=https://coalwire.fly.dev
